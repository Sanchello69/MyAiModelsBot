# Быстрый старт Bitcoin Monitor

## 🚀 Запуск за 3 шага

### Шаг 1: Запустить MCP сервер
```bash
cd /Users/aleksandrvasilev/McpServer
./start.sh
```

Или вручную:
```bash
npm run http
```

Сервер должен запуститься на `http://localhost:3000`

### Шаг 2: Открыть проект в Android Studio
1. Откройте Android Studio
2. Откройте проект `/Users/aleksandrvasilev/AndroidStudioProjects/MyAiModelsBot`
3. Дождитесь синхронизации Gradle

### Шаг 3: Запустить приложение
1. Выберите эмулятор или подключите реальное устройство
2. Нажмите "Run" (зеленая стрелка)
3. В приложении перейдите на вкладку "Bitcoin Monitor"
4. Нажмите кнопку "Запустить"

## ✅ Что должно работать

- **Chat**: Общение с AI моделью DeepSeek V3.1
- **Bitcoin Monitor**:
  - Текущий курс BTC обновляется каждые 30 секунд
  - AI анализирует изменения курса
  - История последних 20 проверок
  - Уведомление с кратким анализом

## 📱 Для реального устройства

Если используете реальное Android устройство (не эмулятор):

1. Узнайте IP адрес вашего Mac:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

2. Измените адрес в файле `SimpleMcpClient.kt`:
```kotlin
private val baseUrl: String = "http://YOUR_MAC_IP:3000"
```

3. Убедитесь, что устройство и Mac в одной Wi-Fi сети

## 🔧 Проверка работы

### Проверить MCP сервер:
```bash
curl http://localhost:3000/health
curl http://localhost:3000/bitcoin/price
```

### Логи Android приложения:
Откройте Logcat в Android Studio и фильтруйте по:
- `SimpleMcpClient` - запросы к MCP
- `BitcoinMonitorService` - работа фонового мониторинга
- `BitcoinMonitorViewModel` - обновления UI

## 📝 API ключи

Не забудьте настроить:

1. **CoinCap API**: В файле `/Users/aleksandrvasilev/McpServer/.env`
```
COINCAP_API_KEY=your_key
```

2. **OpenRouter API**: В файле `local.properties` проекта Android
```
OPENROUTER_API_KEY=your_key
```

## 🎯 Что реализовано

- [x] Удален выбор модели (используется только DeepSeek V3.1)
- [x] MCP сервер с HTTP wrapper
- [x] Получение курса биткоина через MCP
- [x] Фоновый мониторинг каждые 30 секунд
- [x] AI анализ изменений курса
- [x] Сохранение истории в БД
- [x] UI с отображением текущего курса и анализа
- [x] Foreground service с уведомлениями

## 🐛 Troubleshooting

**MCP сервер не запускается:**
```bash
cd /Users/aleksandrvasilev/McpServer
npm install
npm run http
```

**Приложение не подключается:**
- Проверьте, что MCP сервер запущен
- Для эмулятора используется `10.0.2.2:3000`
- Для реального устройства - IP вашего Mac

**Нет анализа от AI:**
- Проверьте наличие `OPENROUTER_API_KEY` в `local.properties`
- Проверьте логи на ошибки API

**База данных:**
- При изменении схемы БД приложение пересоздает таблицы автоматически
- Для очистки БД: удалите приложение и установите заново
